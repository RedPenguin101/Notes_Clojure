(ns clojureapplied.chapter3)

(comment "Chapter 3: Processing sequential data
         * mapping
         * reducing
         * Filtering
         * taking and dropping
         * sorting, removing dups
         * grouping")

(comment 
  "transforming collections.
  Clojure's philosophy is to think at the level of the collection, aggregate
  instead of looping over elements in a collection")

(comment "Maps")

(comment
  "Maps applies a function to every element in a sequence, producing a new
  sequence of the resulting outputs.")
